# 单元测试报告

[toc]

## 单元测试用例编号方式及对照表

首字母为测试用例所属模块，后跟 3 位十进制数。最后一位为 0 代表正常/成功路径测试，为 1-9 代表异常、边界或失败输入测试。高两位代表该模块内的用例序号。

模块对照表：

| 字母 | 对应模块 |
| --- | --- |
| A | 认证参数校验单元测试 |
| M | 会员等级规则单元测试 |
| O | 火车票与度假订单规则单元测试 |
| P | 积分与会员积分规则单元测试 |
| I | AI 解析与降级规则单元测试 |
| C | 通用 CRUD 控制器单元测试 |
| F | 用户 ID 格式化工具单元测试 |

## 测试结果汇总

| 指标 | 数量 |
| --- | ---: |
| 单元测试总数 | 84 |
| 通过数 | 84 |
| 失败数 | 0 |
| 错误数 | 0 |
| 跳过数 | 0 |
| 单元测试累计耗时 | 1.793 秒 |

## 运行环境

| 项目 | 环境 |
| --- | --- |
| 操作系统 | Windows 11 10.0 amd64 |
| Java 版本 | Oracle JDK 21.0.10 LTS |
| Maven 版本 | Apache Maven 3.9.12 |
| 测试框架 | JUnit 5、Mockito、AssertJ |
| 构建工具 | Maven Surefire Plugin 3.5.5 |
| 运行日期 | 2026-08-28 |

## 运行命令

```powershell
mvn -Dtest="*UnitTest,AIServiceImplTest,CrudControllerTest,UserIdFormatterTest" test
```

全量回归验证命令：

```powershell
mvn test
```

## 模块方法测试

### 认证参数校验单元测试

测试名称 邮箱账号去空格并转为小写
测试编号 A000
测试函数 `normalizeAccountShouldTrimAndLowercaseEmail`
输入数据 `"  USER@QQ.COM  "`
前置条件 直接实例化 `AuthValidationService`，mock `UserRepositoryImpl`
预期输出 返回 `user@qq.com`
实际输出 测试通过，返回值符合预期

测试名称 昵称账号保持大小写  
测试编号 A010  
测试函数 `normalizeAccountShouldKeepNicknameCase`  
输入数据 `"AliceNick"`  
前置条件 直接实例化认证校验服务  
预期输出 昵称不按邮箱规则转小写  
实际输出 测试通过，昵称大小写被保留  

测试名称 昵称去除首尾空格  
测试编号 A020  
测试函数 `normalizeNicknameShouldTrim`  
输入数据 `"  旅行者  "`  
前置条件 直接实例化认证校验服务  
预期输出 返回 `旅行者`  
实际输出 测试通过，首尾空格已去除  

测试名称 昵称为空和超过 30 字符失败  
测试编号 A031  
测试函数 `normalizeNicknameShouldRejectBlankAndTooLongValues`  
输入数据 空字符串、超过 30 字符昵称  
前置条件 直接实例化认证校验服务  
预期输出 抛出 `ApiException`  
实际输出 测试通过，非法昵称被拒绝  

测试名称 合法强密码通过  
测试编号 A040  
测试函数 `validatePasswordShouldAcceptStrongPassword`  
输入数据 符合长度、大小写和特殊字符要求的密码  
前置条件 直接实例化认证校验服务  
预期输出 不抛异常  
实际输出 测试通过  

测试名称 非法密码失败  
测试编号 A051  
测试函数 `validatePasswordShouldRejectInvalidPassword(String)  `
输入数据 6 组参数：空密码、弱密码、过短密码、无大写、无小写、无特殊字符  
前置条件 直接实例化认证校验服务  
预期输出 均抛出 `ApiException`  
实际输出 测试通过，6 组非法密码均被拒绝  

测试名称 合法邮箱通过  
测试编号 A060  
测试函数 `normalizeAndValidateEmailShouldAcceptAllowedAndEduDomains`  
输入数据 普通白名单邮箱、`.edu.cn` 邮箱  
前置条件 直接实例化认证校验服务  
预期输出 邮箱格式校验通过并返回规范化邮箱  
实际输出 测试通过  

测试名称 非法邮箱格式和域名失败  
测试编号 A071  
测试函数 `normalizeAndValidateEmailShouldRejectInvalidOrUnsupportedEmail(String)`  
输入数据 3 组参数：非法格式邮箱、不支持域名邮箱、空邮箱  
前置条件 直接实例化认证校验服务  
预期输出 均抛出 `ApiException`  
实际输出 测试通过，非法邮箱均被拒绝  

测试名称 国家区号自动补加号  
测试编号 A080  
测试函数 `normalizeCountryCodeShouldAddPlus`  
输入数据 `86`  
前置条件 直接实例化认证校验服务  
预期输出 返回 `+86`  
实际输出 测试通过  

测试名称 非法国家区号失败  
测试编号 A091  
测试函数 `normalizeCountryCodeShouldRejectInvalidValues(String)`  
输入数据 4 组非法国家区号  
前置条件 直接实例化认证校验服务  
预期输出 均抛出 `ApiException`  
实际输出 测试通过，4 组非法区号均被拒绝  

测试名称 手机号去除空格和连字符  
测试编号 A100  
测试函数 `normalizePhoneShouldRemoveSeparators`  
输入数据 含空格和 `-` 的手机号  
前置条件 直接实例化认证校验服务  
预期输出 返回纯数字手机号  
实际输出 测试通过  

测试名称 手机号非法格式失败  
测试编号 A111  
测试函数 `normalizePhoneShouldRejectInvalidValues(String)`  
输入数据 3 组参数：过短、过长、包含字母  
前置条件 直接实例化认证校验服务  
预期输出 均抛出 `ApiException`  
实际输出 测试通过，非法手机号均被拒绝  

测试名称 昵称、邮箱、手机号已存在时返回冲突  
测试编号 A121  
测试函数 `ensureAvailabilityShouldRejectExistingIdentityValues`  
输入数据 mock 仓储返回昵称、邮箱或手机号已存在  
前置条件 mock `UserRepositoryImpl`  
预期输出 抛出 409 `ApiException`  
实际输出 测试通过，重复身份信息被拒绝  

测试名称 登录账号按邮箱或昵称调用正确仓储方法  
测试编号 A130  
测试函数 `findLoginUserShouldRouteByEmailOrNickname`  
输入数据 邮箱账号、昵称账号  
前置条件 mock `UserRepositoryImpl`  
预期输出 邮箱调用邮箱查询，昵称调用昵称查询  
实际输出 测试通过，仓储调用路径正确  

测试名称 注册请求必须包含验证码和图形验证码  
测试编号 A141  
测试函数 `validateRegistrationRequestShouldRequireVerificationAndCaptcha`  
输入数据 缺少验证码或 captcha 的注册请求  
前置条件 直接实例化认证校验服务  
预期输出 抛出 `ApiException`  
实际输出 测试通过  

测试名称 登录请求必须包含图形验证码  
测试编号 A151  
测试函数 `validateLoginRequestShouldRequireCaptcha`  
输入数据 缺少 captcha 的登录请求  
前置条件 直接实例化认证校验服务  
预期输出 抛出 `ApiException`  
实际输出 测试通过  

### 会员等级规则单元测试

测试名称 默认积分阈值解析会员等级  
测试编号 M000  
测试函数 `resolveLevelByPointsShouldUseDefaultThresholds(int, short)`  
输入数据 7 组积分：0、99、100、499、500、999、1000  
前置条件 直接实例化 `MembershipService`  
预期输出 分别返回对应会员等级  
实际输出 测试通过，7 组边界积分均符合预期  

测试名称 计算下一等级差额和权益  
测试编号 M010  
测试函数 `getUpgradeInfoShouldReturnNextLevelGapAndBenefits`  
输入数据 当前积分低于下一等级阈值  
前置条件 使用默认会员规则  
预期输出 返回下一等级、所需积分和权益信息  
实际输出 测试通过  

测试名称 已达到最高等级时所需积分为 0  
测试编号 M020  
测试函数 `getUpgradeInfoShouldReturnZeroWhenAlreadyAtMaxLevel`  
输入数据 最高等级积分  
前置条件 使用默认会员规则  
预期输出 下一等级所需积分为 0  
实际输出 测试通过  
  
测试名称 自定义等级配置覆盖默认规则  
测试编号 M030  
测试函数 `customLevelConfigShouldOverrideDefaults`  
输入数据 自定义等级阈值配置  
前置条件 注入自定义会员配置  
预期输出 按自定义阈值解析等级  
实际输出 测试通过  

### 火车票与度假订单规则单元测试

测试名称 学生票应付金额为原价 60%  
测试编号 O000  
测试函数 `createTrainOrderShouldApplyStudentDiscount`  
输入数据 原价 100，学生票标记为 true  
前置条件 mock `OrderMapper`、`ProductMapper`、`PointsMembershipService`  
预期输出 应付金额 60，订单待支付，支付截止时间为创建后 10 分钟  
实际输出 测试通过  

测试名称 未勾选学生且年龄小于 18 岁按儿童票 80%  
测试编号 O010  
测试函数 `createTrainOrderShouldApplyChildDiscountWhenNotStudent`  
输入数据 原价 100，年龄 12，学生票 false  
前置条件 mock 产品余票充足  
预期输出 应付金额 80，extra 中票种为 CHILD  
实际输出 测试通过

测试名称 成人普通票按原价计算  
测试编号 O020  
测试函数 `createTrainOrderShouldUseFullPriceForAdult`  
输入数据 原价 100，成年乘客，非学生  
前置条件 mock 产品余票充足  
预期输出 应付金额 100  
实际输出 测试通过

测试名称 座位类型不适用时拒绝下单  
测试编号 O031  
测试函数 `createTrainOrderShouldRejectUnsupportedSeatType`  
输入数据 选择产品不支持的座位类型  
前置条件 mock 产品 `categoryTags` 不包含该座位类型  
预期输出 抛出 `IllegalArgumentException`  
实际输出 测试通过

测试名称 余票不足时拒绝下单  
测试编号 O041  
测试函数 `createTrainOrderShouldRejectSoldOutProduct`  
输入数据 已售罄产品  
前置条件 mock 库存扣减失败  
预期输出 抛出 `IllegalArgumentException`  
实际输出 测试通过

测试名称 度假产品无取消险时应付金额等于基础金额  
测试编号 O050  
测试函数 `createVacationOrderShouldUseBaseAmountWithoutInsurance`  
输入数据 度假产品单价、游客人数、不购买取消险  
前置条件 mock 度假产品可售  
预期输出 应付金额等于单价乘人数  
实际输出 测试通过

测试名称 度假产品购买取消险时增加 5%  
测试编号 O060  
测试函数 `createVacationOrderShouldAddFivePercentInsurance`  
输入数据 度假产品单价、游客人数、购买取消险  
前置条件 mock 度假产品可售  
预期输出 应付金额为基础金额加 5% 保险费  
实际输出 测试通过

测试名称 订单不存在或已取消时支付失败  
测试编号 O071  
测试函数 `payOrderShouldFailWhenOrderMissingOrCanceled`  
输入数据 不存在订单号、已取消订单号  
前置条件 mock `OrderMapper` 返回 null 或取消状态订单  
预期输出 抛出 `IllegalArgumentException`  
实际输出 测试通过

测试名称 已支付订单重复支付保持幂等  
测试编号 O080  
测试函数 `payOrderShouldBeIdempotentForPaidOrder`  
输入数据 已支付订单  
前置条件 mock 已支付订单数据  
预期输出 返回原订单状态，不重复增加积分  
实际输出 测试通过

测试名称 超过 10 分钟的订单支付时取消  
测试编号 O091  
测试函数 `payOrderShouldCancelExpiredPendingOrder`  
输入数据 创建时间超过 10 分钟的待支付订单  
前置条件 使用固定 `Clock`  
预期输出 订单被取消并抛出超时异常  
实际输出 测试通过
  
测试名称 火车票发车前十五天以上全额退款  
测试编号 O100  
测试函数 `refundTrainOrderShouldRefundAllMoreThanFifteenDaysBeforeDeparture`  
输入数据 已支付火车票订单，发车时间距离固定当前时间超过 15 天  
前置条件 mock 退款更新成功  
预期输出 退款金额等于支付金额，库存回补，积分撤销  
实际输出 测试通过  

测试名称 火车票发车前十五天以内退款 50%  
测试编号 O111  
测试函数 `refundTrainOrderShouldRefundHalfWithinFifteenDaysBeforeDeparture`  
输入数据 已支付火车票订单，发车时间距离固定当前时间不足 15 天  
前置条件 mock 退款更新成功  
预期输出 退款金额为支付金额 50%  
实际输出 测试通过  

测试名称 非法取票码格式直接拒绝  
测试编号 O121  
测试函数 `refundTrainOrderByPickupCodeShouldRejectInvalidCodeBeforeDatabaseQuery`  
输入数据 非 6 位取票码  
前置条件 mock `OrderMapper`  
预期输出 抛出 `IllegalArgumentException`，不查询数据库  
实际输出 测试通过

测试名称 度假订单购买取消险时全额退款  
测试编号 O130  
测试函数 `refundVacationOrderShouldRefundAllWhenInsured`  
输入数据 已支付度假订单，extra 中取消险为 true  
前置条件 mock 退款更新成功  
预期输出 全额退款、库存回补、积分撤销  
实际输出 测试通过

测试名称 未购买取消险时退款 50%  
测试编号 O141  
测试函数 `refundVacationOrderShouldRefundHalfWhenUninsured`  
输入数据 已支付度假订单，extra 中取消险为 false  
前置条件 mock 退款更新成功  
预期输出 退款金额为支付金额 50%  
实际输出 测试通过

测试名称 非已支付订单和重复退款不能退款  
测试编号 O151  
测试函数 `refundVacationOrderShouldRejectUnpaidAndRepeatedRefunds`  
输入数据 待支付订单、已退款订单  
前置条件 mock 不同订单状态  
预期输出 抛出 `IllegalArgumentException`  
实际输出 测试通过

测试名称 AI 行程助手只允许已支付度假订单使用  
测试编号 O161  
测试函数 `generateVacationAssistantShouldRequirePaidVacationOrder`  
输入数据 非已支付或非度假订单  
前置条件 mock 订单查询结果  
预期输出 抛出 `IllegalArgumentException`  
实际输出 测试通过

测试名称 AI 调用失败时返回本地降级建议  
测试编号 O170  
测试函数 `generateVacationAssistantShouldUseAiContentOrFallbackOnFailure`  
输入数据 已支付度假订单，AI 服务异常  
前置条件 mock `ConversationService` 抛出异常  
预期输出 返回本地天气、穿衣、美食建议兜底内容  
实际输出 测试通过

### 积分与会员积分规则单元测试

测试名称 不同金额计算奖励积分并向下取整  
测试编号 P000  
测试函数 `calculateRewardPointsShouldRoundDown(String, int)`  
输入数据 6 组金额：0、负数、49.99、50、680 等  
前置条件 直接实例化 `PointsMembershipService`  
预期输出 不足 50 元为 0 分，50 元为 1 分，680 元为 13 分  
实际输出 测试通过，6 组金额均符合预期

测试名称 null 金额获得 0 积分  
测试编号 P010  
测试函数 `calculateRewardPointsShouldReturnZeroForNullAmount`  
输入数据 `null`  
前置条件 直接实例化积分服务  
预期输出 返回 0  
实际输出 测试通过

测试名称 支付后增加积分、刷新等级并写入日志  
测试编号 P020  
测试函数 `awardPointsShouldUpdateUserLevelAndWriteLog`  
输入数据 用户 ID、订单 ID、业务来源、支付金额  
前置条件 mock 用户仓储、会员服务、积分日志仓储  
预期输出 用户积分增加，等级刷新，写入积分日志  
实际输出 测试通过

测试名称 金额不足以产生积分时不调用仓储  
测试编号 P030  
测试函数 `changePointsShouldSkipRepositoryWhenNoRewardPoints`  
输入数据 不足 50 元金额  
前置条件 mock 仓储依赖  
预期输出 不更新用户，不写积分日志  
实际输出 测试通过

测试名称 退款扣分不能使积分小于 0  
测试编号 P041  
测试函数 `revokePointsShouldNotDropBalanceBelowZero`  
输入数据 用户当前积分低于应扣积分  
前置条件 mock 用户当前积分  
预期输出 积分最低扣到 0  
实际输出 测试通过

测试名称 refreshLevel 根据积分更新等级  
测试编号 P050  
测试函数 `refreshLevelShouldResolveAndPersistCurrentLevel`  
输入数据 用户当前积分  
前置条件 mock `MembershipService` 返回对应等级  
预期输出 用户等级被更新并持久化  
实际输出 测试通过

### AI 解析与降级规则单元测试

测试名称 正常酒店推荐 JSON 解析  
测试编号 I000  
测试函数 `parseHotelIntentFromModelJson`  
输入数据 AI 返回合法酒店推荐 JSON  
前置条件 mock `AIClient`  
预期输出 正确解析城市、预算、设施等字段  
实际输出 测试通过

测试名称 Markdown 代码块包裹 JSON 且价格为字符串  
测试编号 I010  
测试函数 `parseHotelIntentFromMarkdownJsonAndStringPrice`  
输入数据 Markdown fenced JSON，价格字段为字符串  
前置条件 mock AI 返回内容  
预期输出 去除代码块并正确解析价格  
实际输出 测试通过

测试名称 AI 返回空字符串  
测试编号 I021  
测试函数 `parseHotelIntentShouldReturnEmptyIntentForBlankJson`  
输入数据 空字符串  
前置条件 直接调用解析逻辑  
预期输出 返回空意图对象，不抛异常  
实际输出 测试通过

测试名称 酒店推荐非法 JSON 失败  
测试编号 I031  
测试函数 `parseHotelIntentShouldRejectInvalidJson`  
输入数据 非法 JSON  
前置条件 直接调用解析逻辑  
预期输出 抛出解析异常或进入受控失败分支  
实际输出 测试通过

测试名称 正常评论摘要 JSON 解析  
测试编号 I040  
测试函数 `parseReviewSummaryFromModelJson`  
输入数据 AI 返回合法评论摘要 JSON  
前置条件 mock AI 返回内容  
预期输出 正确解析优点、缺点、总结字段  
实际输出 测试通过

测试名称 评论摘要空数组和代码块 JSON 解析  
测试编号 I050  
测试函数 `parseReviewSummaryShouldHandleEmptyArraysAndWrappedContent`
输入数据 Markdown fenced JSON，`pros`、`cons` 为空数组  
前置条件 直接调用解析逻辑  
预期输出 正确解析空数组，不抛异常  
实际输出 测试通过

测试名称 评论摘要非法 JSON 失败  
测试编号 I061  
测试函数 `parseReviewSummaryShouldRejectInvalidJson`  
输入数据 非法 JSON  
前置条件 直接调用解析逻辑  
预期输出 抛出解析异常或进入受控失败分支  
实际输出 测试通过

测试名称 AIClient 抛异常时酒店推荐走降级逻辑  
测试编号 I070  
测试函数 `recommendHotelShouldFallbackWhenAiClientThrows`  
输入数据 酒店推荐请求，AIClient 抛出异常  
前置条件 mock `AIClient`  
预期输出 返回本地兜底推荐  
实际输出 测试通过

测试名称 AIClient 不可用时评论摘要走降级逻辑  
测试编号 I080  
测试函数 `generateReviewSummaryShouldFallbackWhenAiClientUnavailable`
输入数据 评论文本，AI 配置不可用  
前置条件 mock AI 不可用  
预期输出 返回本地摘要  
实际输出 测试通过

测试名称 旅行计划生成 AI 异常时走降级逻辑  
测试编号 I090  
测试函数 `generateTravelPlanShouldFallbackWhenAiClientThrows`  
输入数据 目的地、天数、偏好信息  
前置条件 mock AIClient 抛出异常  
预期输出 返回本地兜底行程规划  
实际输出 测试通过

### 通用 CRUD 控制器单元测试

测试名称 查询、创建、更新请求委托给 Service  
测试编号 C000  
测试函数 `listGetCreateAndUpdateShouldDelegateToService`  
输入数据 list、get、create、update 请求参数  
前置条件 mock 通用 CRUD Service  
预期输出 Controller 调用对应 Service 方法并返回结果  
实际输出 测试通过

测试名称 Service 返回 false 时响应数据为 false  
测试编号 C010  
测试函数 `deleteShouldReturnFalseWhenServiceReturnsFalseAndCallOnce`  
输入数据 删除请求参数  
前置条件 mock Service 删除返回 false  
预期输出 响应 data 为 false，Service 只调用一次  
实际输出 测试通过

测试名称 payload 与 params 同时存在时优先 payload  
测试编号 C020  
测试函数 `deleteShouldUsePayloadWhenPayloadProvided`  
输入数据 request payload 和 query params 同时存在  
前置条件 mock Service  
预期输出 使用 payload 中的参数  
实际输出 测试通过

测试名称 payload 为 null 或空时使用 params  
测试编号 C030  
测试函数 `deleteShouldUseParamsWhenPayloadIsNullOrEmpty`  
输入数据 payload 为空，query params 存在  
前置条件 mock Service  
预期输出 使用 query params  
实际输出 测试通过

测试名称 Service 抛异常时不被 Controller 吞掉  
测试编号 C041  
测试函数 `deleteShouldPropagateServiceException`  
输入数据 删除请求参数  
前置条件 mock Service 抛出异常  
预期输出 异常向上抛出  
实际输出 测试通过  

### 用户 ID 格式化工具单元测试

测试名称 正常 16 位用户 ID 格式化  
测试编号 F000  
测试函数 `testFormat16`  
输入数据 合法用户 ID  
前置条件 直接调用 `UserIdFormatter`  
预期输出 返回 16 位格式化字符串  
实际输出 测试通过

测试名称 用户 ID 边界输入格式化  
测试编号 F011  
测试函数 `testFormat16BoundaryInputs`  
输入数据 null、空字符串、负数、超过 16 位数字、含空格数字、非数字字符串  
前置条件 直接调用 `UserIdFormatter`  
预期输出 对非法或边界输入返回受控结果，不发生非预期异常  
实际输出 测试通过
